---
title: "NPI/INDC policies"
author: "FH"
date: "3/10/2017"
output: pdf_document

---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(magpie)
library(reshape2)
library(knitr)
clean_indc <- function(x) {
  x[getRegions(which(x[,,"indc"] == 0,arr.ind = TRUE)),,] <- NA
  #x <- x[,,"targettype",invert=TRUE]
  indc_true <- sort(unique(getRegions(which(x[,,"indc"] == 1,arr.ind = TRUE))))
  x <- x[indc_true,,]
  x <- x[,,"indc",invert=TRUE]
  getNames(x,dim=1) <- c("Ref","BaseYear","EndYear","Target")
  x <- as.data.frame(x)
  x$Cell <- NULL
  x$Year <- NULL
  x <- dcast(x, Region ~ Data1, value.var="Value")
  return(x)
}
load("data_out/npi_indc_country.RData")
```

## Reduced deforestation (%)
percentage reduction: 0 = no reduction, 1 = full reduction of deforestation

Ref: BaseYear (1), BAU scenario (2)

```{r AD, results='asis'}
options(knitr.kable.NA = '')
cat(c("\\begin{table}[!htb]
    \\begin{minipage}{.5\\linewidth}
      \\caption{NPI}
      \\centering",
        kable(clean_indc(npi_pol_deforest),digits = 2,format = "latex",booktabs=TRUE,linesep = c("")),
    "\\end{minipage}%
    \\begin{minipage}{.5\\linewidth}
      \\centering
        \\caption{INDC}",
        kable(clean_indc(indc_pol_deforest),digits = 2,format="latex",booktabs=TRUE,linesep = c("")),
    "\\end{minipage} 
\\end{table}"
))  
```

## Afforestation (Mha)
Ref: BaseYear (1), BAU scenario (2)

```{r AFF, results='asis'}
options(knitr.kable.NA = '')
cat(c("\\begin{table}[!htb]
    \\begin{minipage}{.5\\linewidth}
      \\caption{NPI}
      \\centering",
        kable(clean_indc(npi_pol_afforest),digits = 2,format = "latex",booktabs=TRUE,linesep = c("")),
    "\\end{minipage}%
    \\begin{minipage}{.5\\linewidth}
      \\centering
        \\caption{INDC}",
        kable(clean_indc(indc_pol_afforest),digits = 2,format="latex",booktabs=TRUE,linesep = c("")),
    "\\end{minipage} 
\\end{table}"
))  
```

## CO2 emission reduction (%)
percentage reduction: 0 = no reduction, 1 = full reduction of CO2 emissions from deforestation

Ref: BaseYear (1), BAU scenario (2)

```{r EMIS, results='asis'}
options(knitr.kable.NA = '')
kable(clean_indc(npi_pol_emis),digits = 2,caption = "NPI")
kable(clean_indc(indc_pol_emis),digits = 2,caption = "INDC")
```
